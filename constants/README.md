# Constants

Canonical runtime-safe constants live in `packages/constants` and are imported as `@powerchain/constants`. This root directory is retained only as a compatibility/documentation surface; do not add a second source of truth here.
