# Shared compatibility surface

Canonical reusable React implementations live in `packages/ui`. This directory only exposes narrow compatibility re-exports for paths used by product specifications; do not duplicate component implementations here.
