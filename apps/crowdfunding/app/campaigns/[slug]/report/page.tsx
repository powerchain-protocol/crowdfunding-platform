import { notFound } from "next/navigation";
import { AppShell, Card, Icon, PageHeader, Select, Textarea } from "@powerchain/ui";
import { campaigns } from "../../../../lib/campaigns";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const campaign=campaigns.find((item)=>item.slug===slug);if(!campaign)notFound();return <AppShell appName="Report Campaign" nav={[{label:"Back to campaign",href:`/campaigns/${slug}`},{label:"Explore",href:"/explore"}]}><div className="mx-auto max-w-2xl"><PageHeader eyebrow="Community safety" title={`Report ${campaign.title}`} description="Reports enter the moderation queue. Critical policy matches can trigger a reversible auto-suspension while a moderator reviews the case."/><Card className="mt-7 p-6"><label className="block text-sm font-bold">Reason<Select className="mt-2" defaultValue=""><option value="" disabled>Select a reason</option><option value="FRAUD">Suspected fraud</option><option value="MISLEADING">Misleading information</option><option value="IMPERSONATION">Impersonation</option><option value="PROHIBITED_CONTENT">Prohibited content</option><option value="SPAM">Spam / duplicate</option><option value="OTHER">Other</option></Select></label><label className="mt-5 block text-sm font-bold">Details<Textarea className="mt-2" placeholder="Describe what you observed. Do not include sensitive personal information unless necessary for the report."/></label><div className="mt-5 rounded-xl border border-amber-200 bg-amber-50 p-3 text-xs leading-5 text-amber-900"><strong>Submission boundary:</strong> the production API must persist the report, enforce rate limits and append an audit event before the report is considered received.</div><button type="button" disabled className="mt-5 inline-flex h-10 items-center gap-2 rounded-xl bg-neutral-300 px-4 text-sm font-bold text-white"><Icon name="flag"/> Submit report after API wiring</button></Card></div></AppShell>}
