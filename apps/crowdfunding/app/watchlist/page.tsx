import { AppShell, Card, Icon, LocalizedText, PageHeader, StatusBadge } from "@powerchain/ui";
import { listingFixtures, listingGoal } from "../../lib/listings";
export default function Page(){return <AppShell appName={<LocalizedText en="Investor Watchlist" es="Seguimiento del inversor"/>} nav={[{label:<LocalizedText en="Marketplace" es="Mercado"/>,href:"/marketplace"},{label:<LocalizedText en="Watchlist" es="Seguimiento"/>,href:"/watchlist"}]}><PageHeader eyebrow={<LocalizedText en="Investor workspace" es="Espacio del inversor"/>} title={<LocalizedText en="Watchlist" es="Lista de seguimiento"/>} description={<LocalizedText en="Save listings for follow-up, diligence and offer review. Production watchlists are private to the authenticated investor under row-level security." es="Guarda oportunidades para seguimiento, debida diligencia y revisión de ofertas. En producción, la lista es privada para el inversor autenticado mediante seguridad a nivel de fila."/>}/><div className="mt-6 grid gap-4">{listingFixtures.slice(0,2).map((item)=><Card key={item.id} className="flex flex-col gap-4 p-5 md:flex-row md:items-center md:justify-between"><div><div className="flex items-center gap-2"><h2 className="font-bold">{item.productName}</h2><StatusBadge tone={item.verified?"success":"warning"}>{item.verified?<LocalizedText en="Verified" es="Verificado"/>:<LocalizedText en="Review" es="Revisión"/>}</StatusBadge></div><p className="mt-1 text-sm text-neutral-500">{item.category} · {item.stage} · €{listingGoal(item).toLocaleString()} <LocalizedText en="derived raise" es="objetivo derivado"/></p></div><div className="flex gap-2"><a href={`/listings/${item.slug}`} className="inline-flex h-10 items-center gap-2 rounded-xl border bg-white px-4 text-sm font-bold"><Icon name="eye"/> <LocalizedText en="Diligence" es="Debida diligencia"/></a><a href={`/deal-room/room_${item.id}`} className="inline-flex h-10 items-center gap-2 rounded-xl bg-emerald-950 px-4 text-sm font-bold text-white"><Icon name="chat"/> <LocalizedText en="Deal room" es="Sala de negociación"/></a></div></Card>)}</div></AppShell>}
