# Campaign Discovery Registry

Stores canonical discoverable campaign references/status, not mutable presentation data. Indexer consumes registry events to serve search/filter APIs. Registry writes require campaign/program authority and campaign PDA validation.
