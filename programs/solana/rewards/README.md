# Rewards Program

Reward minting uses the configured PowerChain crowdfunding reward mint. The mint address is deployment configuration, not a user-supplied parameter. Rewards cannot confer financial rights implicitly and cannot be used as evidence that a campaign payment settled.
