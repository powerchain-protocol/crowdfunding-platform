import {success} from "@powerchain/api-core";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const body=await request.json() as {amountBaseUnits?:string;wallet?:string};if(!body.amountBaseUnits||!/^\d+$/.test(body.amountBaseUnits)||BigInt(body.amountBaseUnits)<=0n)return Response.json({error:{code:"INVALID_AMOUNT",message:"A positive integer base-unit amount is required.",requestId:crypto.randomUUID()}},{status:400});if(!body.wallet)return Response.json({error:{code:"WALLET_REQUIRED",message:"Contributor wallet is required to prepare contribute().",requestId:crypto.randomUUID()}},{status:400});return Response.json(success({campaignId:id,instruction:"contribute",amountBaseUnits:body.amountBaseUnits,wallet:body.wallet,escrow:"resolved-from-chain",feeSnapshot:"resolved-from-campaign",requiresWalletSignature:true,status:"PREPARED"}))}
