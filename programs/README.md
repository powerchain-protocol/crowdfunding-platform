# Programs

On-chain program source belongs under chain-specific directories. Deployment identifiers must be versioned and verified from live network configuration before any write is enabled. This repository does not invent program IDs.
