import { AppShell, Card, Icon, PageHeader, Section, StatusBadge } from "@powerchain/ui";
const docs = process.env.NEXT_PUBLIC_DOCS_URL || "http://localhost:3008";
export default function Page(){return <AppShell accountMode="dashboard" appName="Help" nav={[{label:"Dashboard",href:"/"},{label:"Settings",href:"/settings"},{label:"Wallet",href:"/wallet"}]}><PageHeader eyebrow="Dashboard only" title="Help & operator guidance" description="Short answers for the tasks people most often need while operating a project. Full technical and policy details stay in Docs."/><div className="mt-6 grid gap-4 lg:grid-cols-3">{[{icon:"wallet" as const,title:"Connect a wallet safely",copy:"Use the wallet modal, verify the wallet name and install only from an official wallet domain.",href:`${docs}/get-started#wallets`},{icon:"vault" as const,title:"Release a milestone",copy:"Funding threshold, proof, investor/reviewer approval and treasury policy remain separate gates.",href:`${docs}/get-started#milestones`},{icon:"settings" as const,title:"Use your own RPC",copy:"Add a custom Solana RPC or encrypted Helius API key from Settings → RPC.",href:"/settings?tab=rpc"}].map((item)=><Card key={item.title} className="p-5"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-emerald-50 text-emerald-950"><Icon name={item.icon}/></span><h2 className="mt-4 font-bold">{item.title}</h2><p className="mt-2 text-sm leading-6 text-neutral-500">{item.copy}</p><a href={item.href} className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-emerald-950">Open guidance <Icon name="arrow-right"/></a></Card>)}</div><Section title="When something looks wrong"><Card className="p-5"><div className="flex items-start gap-3"><StatusBadge tone="warning">Do not guess</StatusBadge><p className="text-sm leading-6 text-neutral-600">If a payment, on-chain transaction, identity check or milestone release is uncertain, keep the state pending and reconcile it from authoritative provider/on-chain evidence. Never mark it complete from a redirect, screenshot or client-side event alone.</p></div></Card></Section></AppShell>}
