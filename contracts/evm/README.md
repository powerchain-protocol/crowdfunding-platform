# EVM Contracts

PowerChain prefers audited provider primitives such as Safe and attestation protocols over unnecessary bespoke treasury contracts. Any custom EVM contract requires deployment manifests, audit status and chain-specific verification.
