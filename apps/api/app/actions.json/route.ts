export const dynamic = "force-dynamic";
export function GET() { return Response.json({ version: "1.0.0", name: "PowerChain Actions", actions: [{ id: "campaign.contribute", href: "/api/v1/campaigns/{campaignId}/contribute", walletSignatureRequired: true }, { id: "campaign.refund", href: "/api/v1/refunds/{campaignId}", walletSignatureRequired: true }, { id: "milestone.claim", href: "/api/v1/campaigns/{campaignId}/milestones/{milestoneId}/claim", walletSignatureRequired: true }], note: "Action metadata never authorizes signing; the connected wallet owns final approval." }, { headers: { "cache-control": "public, max-age=300" } }); }
