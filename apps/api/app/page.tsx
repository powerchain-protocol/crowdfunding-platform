import { AppShell, Card, PageHeader, Section, StatusBadge } from "@powerchain/ui";
export default function Page(){return <AppShell appName="API"><PageHeader eyebrow="/api/v1" title="Canonical backend boundary" description="Provider credentials, reconciliation and privileged integrations remain server-side."/><Section title="Development endpoints"><Card className="divide-y">{["GET /api/v1/health","GET /api/v1/system","GET /api/v1/campaigns","GET /api/v1/payment-intents/:id"].map(e=><div key={e} className="flex items-center justify-between p-4"><code className="text-sm">{e}</code><StatusBadge tone="success">Available</StatusBadge></div>)}</Card></Section></AppShell>}
