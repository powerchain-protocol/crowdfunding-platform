import { AppShell, Card, Icon, PageHeader, Section, StatusBadge } from "@powerchain/ui";
import { CampaignGrid } from "../components/campaign-grid";

export default function Page() {
  return <AppShell appName="Crowdfunding" nav={[{ label: "Explore projects", href: "/explore" }, { label: "Projects", href: "/projects" }, { label: "Create", href: "/create" }, { label: "Transparency", href: "http://localhost:3007" }]}><PageHeader eyebrow="Crowdfunding" title="Fund work you can follow." description="Explore humanitarian, disaster-relief, renewable-energy, public-good and ticketed utility projects with transparent treasury controls and milestone evidence." action={<a href="/create" className="inline-flex h-10 items-center gap-2 rounded-xl bg-emerald-950 px-4 text-sm font-bold text-white shadow-sm">Create campaign <Icon name="arrow-right"/></a>} /><div className="mt-6 grid gap-4 md:grid-cols-3">{[["Reconciled funding","Campaign totals update after settlement verification."],["Policy-controlled treasury","Evidence and approvals remain separate release gates."],["Public impact trail","Milestones connect funding to outcomes and proof."]].map(([title,copy],index)=><Card key={title} className="p-5"><div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-emerald-50 text-emerald-950"><Icon name={index===0?"check":index===1?"shield":"file"}/></span><div><div className="text-sm font-bold">{title}</div><div className="mt-1 text-xs leading-5 text-slate-500">{copy}</div></div></div></Card>)}</div><Section title="Active projects" description="Funding totals are presented separately from treasury availability and disbursements." action={<StatusBadge tone="success">4 active</StatusBadge>}><CampaignGrid /></Section></AppShell>;
}
