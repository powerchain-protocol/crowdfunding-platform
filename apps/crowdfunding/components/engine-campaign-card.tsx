import { Card, Countdown, FundingProgress, Icon, StatusBadge } from "@powerchain/ui"; import { AvatarPlaceholder, ProjectLogoPlaceholder } from "./placeholders";
type Campaign={slug:string;title:string;category:string;summary:string;funded:number;goal:number;deadline:string;contributors:number;logoUrl?:string;ownerName?:string;ownerAvatarUrl?:string};
export function EngineCampaignCard({campaign}:{campaign:Campaign}){return <Card className="overflow-hidden"><div className="bg-emerald-950 p-5 text-white"><div className="flex items-start justify-between gap-3"><div className="flex items-center gap-3">{campaign.logoUrl?<img src={campaign.logoUrl} alt="" className="h-11 w-11 rounded-xl bg-white object-contain p-1"/>:<ProjectLogoPlaceholder label={campaign.title}/>}<div><StatusBadge>{campaign.category}</StatusBadge><div className="mt-2 flex items-center gap-2 text-[10px] text-white/60">{campaign.ownerAvatarUrl?<img src={campaign.ownerAvatarUrl} alt="" className="h-6 w-6 rounded-full object-cover"/>:<AvatarPlaceholder label={campaign.ownerName||"Creator"}/>}<span>{campaign.ownerName||"Verified creator"}</span></div></div></div><span className="text-xs font-semibold text-white/60">Open</span></div><h2 className="mt-5 text-xl font-bold tracking-tight">{campaign.title}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-white/65">{campaign.summary}</p></div><div className="p-5"><FundingProgress funded={campaign.funded} goal={campaign.goal}/><div className="mt-4 flex items-center justify-between gap-3"><div><div className="text-[10px] font-bold uppercase tracking-[.12em] text-neutral-400">Time remaining</div><div className="mt-1"><Countdown deadline={campaign.deadline} compact/></div></div><div className="text-right"><div className="font-bold">{campaign.contributors}</div><div className="text-xs text-neutral-500">contributors</div></div></div><a href={`/campaigns/${campaign.slug}`} className="mt-5 flex h-10 items-center justify-center gap-2 rounded-xl bg-emerald-950 text-sm font-bold text-white">View campaign <Icon name="arrow-right"/></a></div></Card>}
