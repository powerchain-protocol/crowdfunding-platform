# Infrastructure

Production infrastructure should provision isolated app origins, API/WAF, Postgres, Redis/queue, object storage, KMS/secret manager, observability and webhook ingress. Provider secrets are server-side only.
