# Contributors Program

Maintains public wallet-level contribution aggregates used for the top-3 on-chain leaderboard. Do not put contributor names, emails or KYC data on-chain. The indexer ranks aggregate contribution amounts and returns public aliases only when separately opted in.
