import { notFound } from "next/navigation";
import { AppShell, Card, Countdown, Icon, ProgressBar, Section, StatusBadge } from "@powerchain/ui";
import { campaigns } from "../../../lib/campaigns";
import { ContributorLeaderboard } from "../../../components/contributor-leaderboard";
import { LiveActivityFeed } from "../../../components/live-activity-feed";
import { DonationPanel } from "../../../components/donation-panel";

export default async function Page({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const campaign = campaigns.find((item) => item.slug === slug);
  if (!campaign) notFound();
  const pct = Math.round(campaign.raised / campaign.goal * 100);
  return <AppShell appName="Campaign" nav={[{label:"Explore projects",href:"/explore"},{label:"Create",href:"/create"},{label:"Explorer",href:"http://localhost:3007"}]}>
    <div className="overflow-hidden rounded-[26px] border border-neutral-200 bg-white shadow-[0_20px_60px_rgba(0,0,0,.06)]"><div className="relative bg-emerald-950 px-6 py-9 text-white sm:px-9 lg:px-10 lg:py-12"><div className="absolute -right-20 -top-24 h-72 w-72 rounded-full bg-white/[.07] blur-3xl"/><div className="relative flex flex-col gap-8 lg:flex-row lg:items-end lg:justify-between"><div className="max-w-3xl"><div className="flex flex-wrap gap-2"><StatusBadge>{campaign.type}</StatusBadge><StatusBadge>{campaign.mode}</StatusBadge></div><h1 className="mt-5 text-4xl font-bold tracking-[-.045em] sm:text-5xl">{campaign.title}</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-white/65 sm:text-base">{campaign.summary}</p><div className="mt-5 flex flex-wrap gap-4 text-xs font-semibold text-white/60"><span className="flex items-center gap-2"><Icon name="globe" className="h-4 w-4"/>{campaign.region}</span><span className="flex items-center gap-2"><Icon name="shield" className="h-4 w-4"/>Verified campaign treasury</span></div><div className="mt-4"><Countdown deadline={campaign.deadline}/></div></div><a href="http://localhost:3002" className="inline-flex h-11 shrink-0 items-center justify-center gap-2 rounded-xl bg-white px-5 text-sm font-bold text-emerald-950 shadow-sm transition hover:-translate-y-px">Support project <Icon name="arrow-right"/></a></div></div>
      <div className="grid gap-0 border-t border-neutral-200 sm:grid-cols-4">{[[`€${campaign.raised.toLocaleString()}`,"Reconciled"],[`€${campaign.goal.toLocaleString()}`,"Funding target"],[`${pct}%`,"Progress"],["2 of 3","Treasury approvals"]].map(([value,label])=><div key={label} className="border-b border-neutral-200 p-5 sm:border-b-0 sm:border-r last:border-r-0"><div className="text-lg font-bold">{value}</div><div className="mt-1 text-[10px] font-bold uppercase tracking-[.1em] text-neutral-400">{label}</div></div>)}</div>
    </div>

    <div className="mt-7 grid gap-6 xl:grid-cols-[1fr_360px]"><div className="space-y-6"><Card className="p-6 sm:p-7"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-emerald-50 text-emerald-950"><Icon name="file" className="h-5 w-5"/></span><div><h2 className="font-bold tracking-tight">Public campaign record</h2><p className="mt-1 text-xs text-neutral-500">What was promised, received, approved and proven.</p></div></div><p className="mt-5 text-sm leading-7 text-neutral-600">{campaign.description}</p><div className="mt-5 grid gap-2 sm:grid-cols-2">{campaign.perks.map(perk=><div key={perk} className="flex items-center gap-2 rounded-xl bg-neutral-50 px-3 py-2.5 text-xs font-semibold text-neutral-700"><Icon name="check" className="h-4 w-4 text-emerald-900"/>{perk}</div>)}</div><p className="mt-5 text-xs leading-5 text-neutral-500">Problem, beneficiaries, budget, milestones, evidence and impact updates stay linked to treasury activity throughout the campaign lifecycle. Sensitive donor and beneficiary information is not exposed in the public record by default.</p></Card>
      <Card className="p-6 sm:p-7"><div className="flex items-end justify-between gap-4"><div><div className="text-[10px] font-bold uppercase tracking-[.14em] text-neutral-400">Funding progress</div><div className="mt-2 flex items-baseline gap-2"><span className="text-2xl font-bold">€{campaign.raised.toLocaleString()}</span><span className="text-sm text-neutral-500">of €{campaign.goal.toLocaleString()}</span></div></div><StatusBadge tone="success">{pct}% funded</StatusBadge></div><div className="mt-4"><ProgressBar value={pct} className="h-2.5"/></div><div className="mt-5 grid gap-3 sm:grid-cols-3">{[["€84,200","Available"],["€74,000","Committed"],["€90,720","Disbursed"]].map(([value,label])=><div key={label} className="rounded-xl bg-neutral-50 p-4"><div className="font-bold">{value}</div><div className="mt-1 text-xs text-neutral-500">{label}</div></div>)}</div></Card>
      <Section title="Milestones & evidence" description="Funding thresholds and evidence approval remain separate release conditions."><div className="grid gap-3"><Card className="p-5"><div className="flex items-start justify-between gap-4"><div className="flex gap-3"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-emerald-950 text-white"><Icon name="check" className="h-4 w-4"/></span><div><div className="text-sm font-bold">M1 · Initial deployment</div><div className="mt-1 text-xs text-neutral-500">Funding reached · evidence verified · release reconciled</div></div></div><StatusBadge tone="success">Complete</StatusBadge></div></Card><Card className="p-5"><div className="flex items-start justify-between gap-4"><div className="flex gap-3"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full border border-amber-200 bg-amber-50 text-xs font-bold text-amber-900">2</span><div><div className="text-sm font-bold">M2 · Next release</div><div className="mt-1 text-xs text-neutral-500">Funding threshold reached · evidence review pending</div></div></div><StatusBadge tone="warning">Evidence pending</StatusBadge></div></Card><Card className="p-5 opacity-75"><div className="flex items-start justify-between gap-4"><div className="flex gap-3"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full border border-neutral-200 bg-white text-xs font-bold text-neutral-400">3</span><div><div className="text-sm font-bold">M3 · Expanded operations</div><div className="mt-1 text-xs text-neutral-500">Future milestone · funding threshold not reached</div></div></div><StatusBadge>Future</StatusBadge></div></Card></div></Section><Section title="On-chain contributor leaderboard" description="The top three public contributor accounts are read from the PowerChain contributors program/indexer; private donor identity is not exposed."><ContributorLeaderboard campaignId={campaign.id}/></Section><Section title="Live activity" description="Latest indexed transaction hashes refresh every 5–15 seconds using realtime fallbacks."><LiveActivityFeed campaignId={campaign.id}/></Section></div>
      <aside className="space-y-4 xl:sticky xl:top-24 xl:h-fit"><DonationPanel campaignId={campaign.id}/><Card className="p-5"><div className="text-[10px] font-bold uppercase tracking-[.14em] text-neutral-400">Identity & trust</div><div className="mt-4 grid gap-2">{[["Business verified","building"],["Bank account verified","bank"],["Treasury control verified","vault"],["Beneficiary verified","id-card"]].map(([label,icon])=><div key={label} className="flex items-center justify-between rounded-xl bg-neutral-50 px-3 py-2.5"><span className="flex items-center gap-2 text-xs font-semibold text-neutral-700"><Icon name={icon as "building"|"bank"|"vault"|"id-card"}/>{label}</span><StatusBadge tone="success">Verified</StatusBadge></div>)}</div><p className="mt-3 text-[10px] leading-4 text-neutral-400">Badges represent individual completed checks; they are not a blanket guarantee of campaign outcomes.</p></Card><Card className="p-5"><div className="flex items-center justify-between"><StatusBadge tone="success">Treasury verified</StatusBadge><span className="text-[10px] font-bold text-neutral-400">EURe</span></div><div className="mt-5 flex items-baseline justify-between"><span className="text-2xl font-bold">€{campaign.raised.toLocaleString()}</span><span className="text-xs text-neutral-500">raised</span></div><div className="mt-3"><ProgressBar value={pct}/></div><p className="mt-3 text-xs leading-5 text-neutral-500">Funding shown here represents reconciled campaign contributions.</p><a href="http://localhost:3002" className="mt-5 flex h-11 items-center justify-center gap-2 rounded-xl bg-emerald-950 px-4 text-sm font-bold text-white shadow-sm">Support this project <Icon name="arrow-right"/></a></Card><Card className="p-5"><div className="text-[10px] font-bold uppercase tracking-[.14em] text-neutral-400">Treasury controls</div><dl className="mt-4 grid grid-cols-[1fr_auto] gap-y-3 text-sm"><dt className="text-neutral-500">Approval policy</dt><dd className="font-semibold">2 of 3</dd><dt className="text-neutral-500">Evidence gating</dt><dd className="font-semibold">Required</dd><dt className="text-neutral-500">Settlement</dt><dd className="font-semibold">EURe</dd></dl><a href="http://localhost:3007" className="mt-5 inline-flex items-center gap-2 text-xs font-bold text-emerald-950">View treasury record <Icon name="arrow-right" className="h-3.5 w-3.5"/></a></Card><Card className="p-5"><div className="flex gap-3"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-emerald-50 text-emerald-950"><Icon name="shield"/></span><div><div className="text-sm font-bold">Transparency without donor exposure</div><p className="mt-1 text-xs leading-5 text-neutral-500">Campaign settlement and treasury activity can remain visible without publishing private supporter identity.</p></div></div></Card><a href={`/campaigns/${campaign.slug}/report`} className="flex items-center justify-center gap-2 rounded-xl border border-neutral-200 bg-white px-4 py-3 text-xs font-bold text-neutral-600 transition hover:border-red-200 hover:bg-red-50 hover:text-red-800"><Icon name="flag"/> Report or flag campaign</a></aside></div>
  </AppShell>;
}
