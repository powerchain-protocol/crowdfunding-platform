const PUBLIC_ORIGIN_KEYS=["NEXT_PUBLIC_WEB_URL","NEXT_PUBLIC_CROWDFUNDING_URL","NEXT_PUBLIC_DONATE_URL","NEXT_PUBLIC_CHECKOUT_URL","NEXT_PUBLIC_DASHBOARD_URL","NEXT_PUBLIC_TREASURY_URL","NEXT_PUBLIC_ADMIN_URL","NEXT_PUBLIC_EXPLORER_URL","NEXT_PUBLIC_DOCS_URL","NEXT_PUBLIC_PWA_URL"] as const;
export function allowedOrigins():string[]{return PUBLIC_ORIGIN_KEYS.map(k=>process.env[k]).filter((x):x is string=>Boolean(x)).map(x=>new URL(x).origin)}
export function assertSameOrigin(request:Request):void {const method=request.method.toUpperCase();if(["GET","HEAD","OPTIONS"].includes(method))return;const origin=request.headers.get("origin");if(!origin)return;const allowed=allowedOrigins();if(allowed.length&& !allowed.includes(new URL(origin).origin))throw new Error("ORIGIN_NOT_ALLOWED")}
export async function readJson<T>(request:Request):Promise<T>{const type=request.headers.get("content-type")||"";if(!type.includes("application/json"))throw new Error("CONTENT_TYPE_REQUIRED");return await request.json() as T}
