# PowerChain Crowdfunding Token Integration

Reward/certificate minting is a post-settlement side effect. Token/credential minting cannot cause campaign funding recognition and must validate the canonical mint/program configuration. Financial rights are never inferred from a reward token.
