# Migrations

Database migrations must be generated from reviewed schema changes and applied through environment-specific deployment workflows. Never use destructive development migration commands against production.
