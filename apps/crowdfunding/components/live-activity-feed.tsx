"use client";
import { useActivityFeed } from "@powerchain/hooks";
import { Card, StatusBadge } from "@powerchain/ui";
export function LiveActivityFeed({campaignId}:{campaignId:string}){const {events,transport}=useActivityFeed(campaignId,10_000);return <Card className="overflow-hidden"><div className="flex items-center justify-between border-b p-4"><div><div className="font-bold">Latest on-chain activity</div><div className="mt-1 text-xs text-neutral-500">Wallet-safe public projection; no KYC or private donor details.</div></div><StatusBadge tone="success">{transport}</StatusBadge></div><div className="divide-y">{events.length?events.slice(0,6).map(e=><div key={e.id} className="flex items-center justify-between gap-3 p-4"><div><div className="text-sm font-bold">{e.kind.replaceAll("_"," ")}</div><div className="mt-1 font-mono text-xs text-neutral-500">{e.signature}</div></div><div className="text-right">{e.wallet?<div className="font-mono text-xs font-semibold">{e.wallet}</div>:null}<div className="mt-1 text-[10px] text-neutral-400">{new Date(e.occurredAt).toLocaleTimeString()}</div></div></div>):<div className="p-5 text-sm text-neutral-500">Waiting for indexed on-chain activity…</div>}</div></Card>}
