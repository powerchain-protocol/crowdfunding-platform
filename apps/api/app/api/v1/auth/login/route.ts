import { NextResponse } from "next/server";
import { authenticateUser, createSession } from "@powerchain/auth/server";
import { SESSION_COOKIE_NAME, SESSION_COOKIE_OPTIONS } from "@powerchain/auth";
import { assertSameOrigin, readJson } from "../../../../../lib/request-security";
export async function POST(request:Request){try{assertSameOrigin(request);const body=await readJson<{email?:string;password?:string}>(request);if(!body.email||!body.password)return NextResponse.json({error:{code:"VALIDATION_ERROR",message:"Email and password are required.",requestId:crypto.randomUUID()}},{status:400});const user=await authenticateUser(body.email,body.password);if(!user)return NextResponse.json({error:{code:"INVALID_CREDENTIALS",message:"Email or password is incorrect.",requestId:crypto.randomUUID()}},{status:401});const session=await createSession(user.id,Number(process.env.AUTH_SESSION_TTL_SECONDS||43200));const res=NextResponse.json({data:{user:{id:user.id,email:user.email,displayName:user.displayName}},meta:{requestId:crypto.randomUUID()}},{status:200});res.cookies.set(SESSION_COOKIE_NAME,session.token,{...SESSION_COOKIE_OPTIONS,expires:session.expiresAt});return res}catch(error){return NextResponse.json({error:{code:"LOGIN_FAILED",message:error instanceof Error?error.message:"Login failed",requestId:crypto.randomUUID()}},{status:400})}}
