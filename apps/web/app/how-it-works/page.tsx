import { AppShell, Card, PageHeader } from "@powerchain/ui";
export default function Page(){const steps=["Create and verify a campaign","Provision treasury controls","Accept bank or wallet funding","Reconcile settlement into the ledger","Reach a milestone threshold","Attach and verify evidence","Approve and execute disbursement","Publish impact and audit history"];return <AppShell appName="How it works" accountMode="website" authHref="/auth/login" nav={[{label:"Explore projects",href:"http://localhost:3001/explore"},{label:"Create",href:"http://localhost:3001/create"}]}><PageHeader eyebrow="Workflow" title="Money does not move just because a target was reached." description="Funding thresholds, evidence verification and treasury approvals are distinct controls."/><div className="mt-8 grid gap-3">{steps.map((step,index)=><Card key={step} className="flex items-center gap-4 p-4"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-emerald-950 text-sm font-bold text-white">{index+1}</span><span className="font-semibold">{step}</span></Card>)}</div></AppShell>}
