import { AppShell, Card, PageHeader, StatusBadge } from "@powerchain/ui";
export default function Page(){return <AppShell appName="Settings" nav={[{label:"Discover",href:"/explore"},{label:"My donations",href:"/my-donations"}]}><PageHeader eyebrow="Preferences" title="Crowdfunding settings" description="Configure realtime refresh, notifications, wallet behavior and payment preferences."/><div className="mt-7 grid gap-5 lg:grid-cols-2"><Card className="p-6"><h2 className="font-bold">Live activity</h2><label className="mt-5 block text-sm font-semibold">Polling fallback interval<select defaultValue="10000" className="mt-2 h-11 w-full rounded-xl border bg-white px-3"><option value="5000">5 seconds</option><option value="10000">10 seconds</option><option value="15000">15 seconds</option></select></label><p className="mt-3 text-xs leading-5 text-neutral-500">Realtime prefers WebSocket, then SSE, then the selected polling interval.</p></Card><Card className="p-6"><h2 className="font-bold">Payments</h2><div className="mt-4 flex items-center justify-between rounded-xl bg-neutral-50 p-4"><div><div className="text-sm font-semibold">Midtrans checkout</div><div className="mt-1 text-xs text-neutral-500">Hosted payment page when provider mode is enabled.</div></div><StatusBadge>Server controlled</StatusBadge></div><div className="mt-3 flex items-center justify-between rounded-xl bg-neutral-50 p-4"><div><div className="text-sm font-semibold">Demo payment mode</div><div className="mt-1 text-xs text-neutral-500">Shows contact-support flow instead of fabricating a payment.</div></div><StatusBadge tone="warning">Fallback</StatusBadge></div></Card></div></AppShell>}
