export async function GET(){ return Response.json({ error:{ code:"COMPANY_PROFILE_PERSISTENCE_NOT_CONFIGURED", message:"Company profile persistence requires authenticated organization storage and field-level authorization.", requestId:crypto.randomUUID() } }, { status:501 }); }
export async function PUT(){ return Response.json({ error:{ code:"COMPANY_PROFILE_PERSISTENCE_NOT_CONFIGURED", message:"Company profile writes are unavailable until authenticated persistence, object storage and verification-field guards are configured.", requestId:crypto.randomUUID() } }, { status:501 }); }
